% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{bivariate_dp}
\alias{bivariate_dp}
\title{Bivariate Dual Process Signal Detection Model}
\usage{
bivariate_dp(
  varying_source_criteria = FALSE,
  bounded = FALSE,
  new_source_criteria = c("full", "shared"),
  old_levels = NULL,
  varying_re = "shared"
)
}
\arguments{
\item{varying_source_criteria}{If \code{TRUE}, estimate separate source
criteria for each detection confidence level.}

\item{bounded}{If \code{TRUE}, fit the BBDP variant (conditional source mean
clamped at the new-item baseline). If \code{FALSE} (default), fit the BDP
variant with standard bivariate Gaussian familiarity.}

\item{new_source_criteria}{With \code{varying_source_criteria = TRUE}, how source
criteria are structured across detection responses (ignored otherwise).
\code{"full"} (default): every detection response level has its own source
criteria (K1 x (K2-1) in total). \code{"shared"}: responses that judge an item
"new" share a single set of source criteria; requires \code{old_levels}.}

\item{old_levels}{Integer vector of detection response levels (on the
1..K1 scale, e.g. \code{old_levels = 4:6}) that count as "old" judgements; the
rest are "new". Required when \code{new_source_criteria = "shared"}; ignored
otherwise.}

\item{varying_re}{Random-effect structure for varying source criteria
(distinct from \code{new_source_criteria}, which governs the fixed criteria).
\code{"shared"} (default): one source-criterion RE shift shared across all
detection bins. \code{"per_bin"}: a separate RE shift per detection bin.
\code{"full"}: a separate RE for every source threshold within every detection
bin. Requires \code{varying_source_criteria = TRUE} unless \code{"shared"}.}
}
\value{
A \code{broc_family} object.
}
\description{
Bivariate dual-process model: Gaussian familiarity plus discrete item and
source recollection (Starns et al., 2014).
}
\details{
Same source labeling convention as \code{\link[=bivariate_gaussian]{bivariate_gaussian()}}: source A = low
source ratings, source B = high.
\subsection{Likelihood}{

For each old item, the cell probability is a 3-component mixture over
recollection states:
\deqn{P(Y_1 = j,\; Y_2 = k \mid X) = (1 - \lambda)\,P_{\mathrm{F}}(j, k \mid X)
      + \lambda(1 - \lambda_2)\,\mathbf{1}[j = K_1]\,P_{\mathrm{S}}(k \mid X)
      + \lambda \lambda_2\,\mathbf{1}[j = K_1, k = k_X^*]}
where:
\itemize{
\item lambda is the item-recollection probability, lambda_2 is the source-recollection
probability (\code{lambda}, \code{lambda2}).
\item \eqn{P_{\mathrm{F}}(j, k \mid X)} is the bivariate-Gaussian familiarity
cell probability, defined cumulatively as
\eqn{P_{\mathrm{F}}(Y_1 \le j, Y_2 \le k \mid X) = \Phi_2\big((c1_j - \mu_1)/\sigma_1,\;
  (c2_k - \mu_2)/\sigma_2;\; \rho\big)} with 4-corner differencing, taking
per-source \eqn{(\mu_1, \mu_2, \sigma_1, \sigma_2, \rho)} from
\code{\link[=bivariate_gaussian]{bivariate_gaussian()}}.
\item \eqn{P_{\mathrm{S}}(k \mid X)} is the marginal source cell probability
(familiarity integrated out): \eqn{P_{\mathrm{S}}(Y_2 \le k \mid X) =
  \Phi((c2_k - \mu_2)/\sigma_2)} with 1D differencing.
\item \eqn{k_X^*} is the recollection-success source response: \eqn{k_X^* = 1}
for X = A, \eqn{k_X^* = K_2} for X = B. Both signal a correctly
identified source under recollection.
\item For new items: lambda = lambda_2 = 0, so the likelihood reduces to
\eqn{P_{\mathrm{F}}} alone with new-item parameters.
}

In the bounded (BBDP) variant, the bivariate piece uses the source-mean
clamping of Starns et al. (2014); otherwise (BDP) the standard
bivariate normal applies.
}

\subsection{Parameters}{

All parameters of \code{\link[=bivariate_gaussian]{bivariate_gaussian()}} (\code{dprime}, \code{dprime_B}, \code{discrim},
\code{discrim_B}, \code{sigma}, \code{sigma_B}, \code{sigma2}, \code{sigma2_B}, \code{rho}, \code{rho_B},
\code{rho_N}, \code{criterion}, \code{criterion2}) plus:
\describe{
\item{lambda}{(required) Item-recollection probability for Source A. Logit
link.}
\item{lambda_B}{(optional) Item-recollection probability for Source B. If
omitted, constrained \code{lambda_B = lambda}.}
\item{lambda2}{(required) Source-recollection probability for Source A.
Logit link.}
\item{lambda2_B}{(optional) Source-recollection probability for Source B. If
omitted, constrained \code{lambda2_B = lambda2}.}
}
Defaults for the bivariate parameters match \code{\link[=bivariate_gaussian]{bivariate_gaussian()}} -- e.g.
\code{dprime_B} defaults to \code{dprime}, \code{sigma} defaults to fixed at 1,
\code{discrim_B} defaults to mirror-symmetric.
}

\subsection{Formula syntax}{

\if{html}{\out{<div class="sourceCode">}}\preformatted{broc(brf(resp(det_conf, src_conf) | type ~ 1 + (1 | subj),
         discrim ~ 1 + (1 | subj),
         criterion ~ 1 + (1 | subj),
         criterion2 ~ 1 + (1 | subj),
         rho ~ 1,
         lambda ~ 1 + (1 | subj),
         lambda2 ~ 1 + (1 | subj),
         family = bivariate_dp()),
    data = dat)
}\if{html}{\out{</div>}}
}
}
\references{
Starns, J. J., Rotello, C. M., & Hautus, M. J. (2014). Recognition memory
zROC slopes for items with correct versus incorrect source decisions
discriminate the dual process and unequal variance signal detection
models. \emph{Journal of Experimental Psychology: Learning, Memory, and
Cognition}, \emph{40}(5), 1205-1225.
}
\seealso{
\code{\link[=bivariate_gaussian]{bivariate_gaussian()}}, \code{\link[=vrdp2d]{vrdp2d()}}.
}
