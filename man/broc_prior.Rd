% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{broc_prior}
\alias{broc_prior}
\title{Set a Prior on an SDT Model Parameter}
\usage{
broc_prior(prior, class, dpar = NULL, coef = NULL, group = NULL)
}
\arguments{
\item{prior}{The prior as a distribution call, e.g. \code{"normal(0, 1)"},
\code{"student_t(3, 0, 2.5)"}, or \code{"lkj(2)"}. Supported on both backends:
\code{normal}, \code{std_normal}, \code{student_t}, \code{cauchy}, \code{logistic}, \code{gumbel},
\code{double_exponential} (= \code{laplace}), \code{uniform}, \code{beta}, \code{exponential},
\code{gamma}, \code{inv_gamma}, \code{weibull}, \code{lognormal}, the half forms \code{half_normal},
\code{half_cauchy}, \code{half_student_t}, and \code{lkj} (\code{class = "cor"} only). For SD
priors (\code{class = "sd"}/\code{"sds"}) a symmetric distribution is automatically
restricted to positive values. The Stan backend additionally accepts any
distribution in the Stan language.}

\item{class}{Parameter class: \code{"b"} (fixed effects), \code{"sd"} (random-effect
SDs), \code{"cor"} (random-effect correlations), or \code{"sds"} (smooth penalty SDs,
only for models with \code{s()}/\code{t2()} smooth terms).}

\item{dpar}{Distributional parameter the prior applies to, e.g. \code{"dprime"},
\code{"sigma"}, \code{"lambda"}. For criterion priors use \code{"thresh_mid"} / \code{"log_gaps"}
(\code{"thresh_mid2"} / \code{"log_gaps2"} for the second response axis in bivariate
and CDP families). Family-specific aliases (\code{mu}, \code{mu1}, \code{mu2}, \code{rec}, \code{fam},
\code{sigma_R}, \code{sigma_F}) are also accepted. \code{NULL} applies the prior to every
parameter in \code{class}.}

\item{coef}{Optional specific coefficient name (e.g., \code{"conditionread"})}

\item{group}{Optional grouping factor for random effects (e.g., \code{"participant"})}
}
\value{
A \code{broc_prior} object. Combine several with \code{\link[=c]{c()}} and pass to \code{\link[=broc]{broc()}}.
}
\description{
Specify a prior for one class of parameters in a \code{\link[=broc]{broc()}} model: a fixed
effect, a random-effect standard deviation, or a random-effect correlation.
Pass one or more \code{broc_prior()} calls to \code{broc(..., priors = ...)}; combine
several with \code{\link[=c]{c()}}.
}
\details{
Priors are matched from most to least specific: a \code{coef}-level prior overrides
a \code{group}-level one, which overrides a \code{dpar}-level one, which overrides the
global class default. Call \code{\link[=get_broc_prior]{get_broc_prior()}} on a \code{\link[=broc]{broc()}} or \code{\link[=brf]{brf()}} object
(with data) to see which coefficients exist and the current prior for each.

Confidence thresholds are not set directly. They are reparameterized as a
central anchor (\code{thresh_mid}) plus strictly-positive, log-scale spacings
(\code{log_gaps}) between successive thresholds, which enforces ordering. Set
criterion priors via \code{dpar = "thresh_mid"} (overall criterion location) and
\code{dpar = "log_gaps"} (threshold spread).
}
\examples{
# Prior on all d' fixed effects
broc_prior("normal(1, 1)", class = "b", dpar = "dprime")

# Prior on specific d' coefficient
broc_prior("normal(2, 0.5)", class = "b", dpar = "dprime", coef = "conditionread")

# Prior on ALL random effect standard deviations
broc_prior("normal(0, 0.5)", class = "sd")

# Prior on random effect SDs for d' only
broc_prior("normal(0, 0.5)", class = "sd", dpar = "dprime")

# Prior on random effect SDs for d' for participant group only
broc_prior("normal(0, 0.3)", class = "sd", dpar = "dprime", group = "participant")

# Prior on ALL correlations
broc_prior("lkj(2)", class = "cor")

# Prior on criterion correlations for participant
broc_prior("lkj(4)", class = "cor", dpar = "criterion", group = "participant")
}
\seealso{
\code{\link[=get_broc_prior]{get_broc_prior()}}
}
