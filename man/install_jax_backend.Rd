% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_jax.R
\name{install_jax_backend}
\alias{install_jax_backend}
\title{Install the JAX/NumPyro backend into a managed venv}
\usage{
install_jax_backend(
  dir = jax_backend_dir(),
  python = NULL,
  jax_version = JAX_VERSION_PIN,
  numpyro_version = NUMPYRO_VERSION_PIN,
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{dir}{Directory to install into. Default: \code{jax_backend_dir()}.}

\item{python}{Path to the Python executable used to \emph{create} the venv.
Default: auto-detect from PATH (must be Python 3.10+). The venv itself
uses its own bundled interpreter regardless of this choice.}

\item{jax_version, numpyro_version}{Versions to pin. Defaults to
\code{JAX_VERSION_PIN} / \code{NUMPYRO_VERSION_PIN} constants in the package
(the latest mutually-compatible pair as of release).}

\item{overwrite}{If \code{TRUE}, recreate the venv even if it already exists.
Default \code{FALSE} errors if a managed venv is already present.}

\item{quiet}{If \code{TRUE}, suppress pip output.}
}
\value{
Invisibly returns the path to the installed venv's Python.
}
\description{
Creates an isolated Python virtual environment under \code{jax_backend_dir()}
and installs \code{jax} and \code{numpyro} at known-compatible pinned versions. This
never touches the user's system or global Python install. The cmdstanr
analogue is \code{\link[cmdstanr:install_cmdstan]{cmdstanr::install_cmdstan()}}.
}
\details{
Subsequent JAX-backend fits via \code{fit_broc(backend = "jax")} will prefer
this managed venv automatically; no further configuration is needed.
}
\seealso{
\code{\link[=check_jax_backend]{check_jax_backend()}}, \code{\link[=jax_python]{jax_python()}}, \code{\link[=set_jax_python]{set_jax_python()}}.
}
