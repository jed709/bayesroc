% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{evsd}
\alias{evsd}
\title{Equal Variance Signal Detection Model}
\usage{
evsd(link = link_probit())
}
\arguments{
\item{link}{Link function (default: \code{\link[=link_probit]{link_probit()}}). Also supports
\code{\link[=link_logit]{link_logit()}} for logistic CDF.}
}
\value{
A \code{broc_family} object.
}
\description{
Canonical signal detection model. Signal and noise distributions are assumed
normal with equal variance fixed at 1 (Macmillan & Creelman, 2005).
}
\details{
\subsection{Likelihood}{

For an old/new judgment Y on a K-point confidence scale (\code{old} in {0, 1})
with criteria c_1 < c_2 < ... < c_{K-1}:
\deqn{P(Y \le k \mid \mathrm{old}) = \Phi(c_k - \mathrm{old} \cdot d')}
for k = 1, ..., K-1, where \eqn{\Phi} is the link CDF (probit by default).
Cell probabilities by differencing:
\eqn{P(Y = k \mid \mathrm{old}) = P(Y \le k \mid \mathrm{old}) - P(Y \le k - 1 \mid \mathrm{old})}
with \eqn{P(Y \le 0) = 0} and \eqn{P(Y \le K) = 1}. Both new- and
old-item distributions have unit SD.
}

\subsection{Parameters}{

\describe{
\item{dprime}{(required) Sensitivity. Identity link.}
\item{criterion}{(required) K-1 confidence thresholds, parameterized
internally as a mid-anchor \code{thresh_mid} plus K-2 ordered gaps. Identity
link on the gap parameterization.}
}
}

\subsection{Formula syntax}{

\if{html}{\out{<div class="sourceCode">}}\preformatted{broc(brf(conf | old ~ condition + (1 + condition | subj),
         criterion ~ 1 + (1 | subj),
         family = evsd()),
    data = dat)
}\if{html}{\out{</div>}}
}
}
\references{
Macmillan, N. A., & Creelman, C. D. (2005). \emph{Detection theory: A user's
guide} (2nd ed.). Lawrence Erlbaum.
}
\seealso{
\code{\link[=uvsd]{uvsd()}} for unequal-variance extension, \code{\link[=dpsd]{dpsd()}} for dual-process,
\code{\link[=mixture]{mixture()}} for mixture variants.
}
