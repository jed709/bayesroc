% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broc_model.R
\name{broc}
\alias{broc}
\title{Specify a Bayesian SDT Model}
\usage{
broc(
  formula = NULL,
  data = NULL,
  family = NULL,
  priors = NULL,
  encoding_vars = NULL,
  counts = NULL,
  cor_threshold = NULL,
  threads = NULL,
  gap_link = NULL,
  batch_likelihood = TRUE
)
}
\arguments{
\item{formula}{A \code{\link[=brf]{brf()}} object, or a bare two-sided formula
\code{response | is_old ~ predictors}.}

\item{data}{A data frame containing the response, the \code{is_old} indicator, and
every predictor and grouping factor named in \code{formula}.}

\item{family}{The SDT model family: one of \code{\link[=evsd]{evsd()}}, \code{\link[=uvsd]{uvsd()}}, \code{\link[=dpsd]{dpsd()}},
\code{\link[=mixture]{mixture()}}, \code{\link[=source_mixture]{source_mixture()}}, \code{\link[=bivariate_gaussian]{bivariate_gaussian()}}, \code{\link[=bivariate_dp]{bivariate_dp()}},
\code{\link[=vrdp2d]{vrdp2d()}}, \code{\link[=cdp]{cdp()}}, \code{\link[=cumulative]{cumulative()}}, or \code{\link[=bivariate_cumulative]{bivariate_cumulative()}}. May also be
set in \code{\link[=brf]{brf()}}; a value in broc() takes precedence.}

\item{priors}{A prior specification from \code{\link[=broc_prior]{broc_prior()}}. If \code{NULL}, the
family's weakly-informative default priors are used (inspect them with
\code{\link[=get_broc_prior]{get_broc_prior()}}).}

\item{encoding_vars}{Character vector of column names to treat as
encoding-phase (study) manipulations for which lure items do not have a
meaningful value. Listing a column here auto-wraps it in \code{encoding()}. May
also be set in \code{\link[=brf]{brf()}}; a value in broc() takes precedence.}

\item{counts}{Name of a column giving per-row trial counts, for aggregated
(count) data rather than one row per trial. May also be set in \code{\link[=brf]{brf()}}.}

\item{cor_threshold}{If \code{TRUE} (default), random effects on the K-1 thresholds
are modeled jointly (correlated) via the threshold parameterization; \code{FALSE}
treats them as independent.}

\item{threads}{If \code{TRUE}, enable within-chain \code{reduce_sum} threading for the
Stan backend (set the thread count per chain via \code{fit_broc(threads_per_chain=)}).
Default \code{FALSE}.}

\item{gap_link}{Link for threshold gaps: \code{"log"} (default) or \code{"softplus"}.
\code{NULL} uses the family default.}

\item{batch_likelihood}{If \code{TRUE} (default), build the fused batch likelihood
with analytic gradients for the Stan backend.}
}
\value{
A \code{broc_model} object, ready to pass to \code{\link[=fit_broc]{fit_broc()}}.
}
\description{
Builds a model specification from a formula, family, and (optional) priors,
and generates the backend code needed to fit it. \code{fit_broc()} is used to fit
the object created by \code{broc()}.
}
\examples{
\dontrun{
# Unequal-variance model (construction only; pass to fit_broc() to fit)
m <- broc(brf(conf | old ~ cond + (1 | subj),
              criterion ~ 1 + (1 | subj),
              sigma ~ 1,
              family = uvsd()),
          data = dat)

# Bare-formula shorthand (criterion defaults to ~ 1)
broc(conf | old ~ cond, data = dat, family = evsd())
}
}
