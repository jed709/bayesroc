% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_roc.R
\name{plot_roc_curve}
\alias{plot_roc_curve}
\title{Plot Model-Implied ROC Curves}
\usage{
plot_roc_curve(
  fit,
  response = 1,
  group = NULL,
  empirical = TRUE,
  scale = c("probability", "z"),
  ndraws = 200,
  prob = 0.95,
  re_formula = NULL,
  cores = 1
)
}
\arguments{
\item{fit}{A \code{broc_fit} object from \code{\link[=fit_broc]{fit_broc()}}.}

\item{response}{Which response dimension to plot for bivariate families:
\code{1} (default) for detection, \code{2} for source. Ignored for univariate families.
For \code{response = 2}, two ROC curves are plotted (Source A vs New, Source B vs
New).}

\item{group}{Optional character vector of column name(s) from the original data
for per-condition ROC curves (faceted). E.g., \code{group = "condition"}.}

\item{empirical}{If \code{TRUE} (default), overlay empirical ROC points computed
from observed data.}

\item{scale}{\code{"probability"} (default) for standard ROC, \code{"z"} for zROC with
qnorm-transformed axes (linear zROC = Gaussian SDT; slope = 1/sigma for UVSDT).}

\item{ndraws}{Number of posterior draws to use (default 200).}

\item{prob}{Width of credible interval for the ribbon (default 0.95).}

\item{re_formula}{\code{NULL} (default) includes all random effects;
\code{NA} for population-level ROC only.}

\item{cores}{Number of cores for parallel computation.}
}
\value{
A ggplot2 object.
}
\description{
Generates receiver operating characteristic (ROC) curves from a fitted
Bayesian SDT model, with posterior credible bands and optional empirical ROC
points, on standard or zROC (probit-transformed) scales.
}
\examples{
\dontrun{
# Standard ROC
plot_roc_curve(fit)

# zROC (probit-transformed)
plot_roc_curve(fit, scale = "z")

# Bivariate: source dimension ROC
plot_roc_curve(fit_bv, response = 2)

# Per-condition ROC curves
plot_roc_curve(fit, group = "condition")
}
}
