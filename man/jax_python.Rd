% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_jax.R
\name{jax_python}
\alias{jax_python}
\title{Resolve the Python executable used by the JAX backend}
\usage{
jax_python()
}
\value{
Character path to a Python executable, or \code{NULL}.
}
\description{
Returns the Python path that \code{fit_broc(backend = "jax")} will currently use.
Resolution order:
\enumerate{
\item \code{Sys.getenv("BAYESROC_PYTHON")} if set and the file exists (set by
\code{\link[=set_jax_python]{set_jax_python()}} or auto-cached during the last fit / install).
\item The managed venv at \code{jax_backend_dir()} if it exists.
\item \code{Sys.getenv("RETICULATE_PYTHON")} if set and the file exists.
\item Auto-discovery: scans common Python locations and PATH for an install
that can \verb{import jax}. (See \code{find_python_with_jax}.)
Returns \code{NULL} if nothing is found -- call \code{\link[=install_jax_backend]{install_jax_backend()}} then.
}
}
\seealso{
\code{\link[=set_jax_python]{set_jax_python()}}, \code{\link[=check_jax_backend]{check_jax_backend()}}, \code{\link[=install_jax_backend]{install_jax_backend()}}.
}
