% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{uvsd}
\alias{uvsd}
\title{Unequal Variance Signal Detection Model}
\usage{
uvsd(link = link_probit())
}
\arguments{
\item{link}{Link function (default: \code{\link[=link_probit]{link_probit()}}).}
}
\value{
A \code{broc_family} object.
}
\description{
Extends \code{\link[=evsd]{evsd()}} by allowing the signal (old-item) distribution to have a
different variance than the noise (new-item) distribution (Macmillan &
Creelman, 2005).
}
\details{
\subsection{Likelihood}{

Noise distribution N(0, 1); signal distribution N(d', sigma). For an
old/new judgment Y on a K-point scale (\code{old} in {0, 1}) with criteria
c_1 < ... < c_{K-1}:
\deqn{P(Y \le k \mid \mathrm{old} = 0) = \Phi(c_k)}
\deqn{P(Y \le k \mid \mathrm{old} = 1) = \Phi\!\Big(\frac{c_k - d'}{\sigma}\Big)}
for k = 1, ..., K-1.
}

\subsection{Parameters}{

\describe{
\item{dprime}{(required) Sensitivity. Identity link.}
\item{criterion}{(required) K-1 confidence thresholds (mid-anchor +
K-2 ordered gaps). Identity link.}
\item{sigma}{(required) SD of the signal distribution. Log link. To fix
sigma at 1, use \code{\link[=evsd]{evsd()}} instead.}
}
}

\subsection{Formula syntax}{

\if{html}{\out{<div class="sourceCode">}}\preformatted{broc(brf(conf | old ~ condition + (1 | subj),
         criterion ~ 1 + (1 | subj),
         sigma ~ 1 + (1 | subj),
         family = uvsd()),
    data = dat)
}\if{html}{\out{</div>}}
}
}
\references{
Macmillan, N. A., & Creelman, C. D. (2005). \emph{Detection theory: A user's
guide} (2nd ed.). Lawrence Erlbaum.
}
\seealso{
\code{\link[=evsd]{evsd()}}, \code{\link[=dpsd]{dpsd()}}, \code{\link[=mixture]{mixture()}}.
}
