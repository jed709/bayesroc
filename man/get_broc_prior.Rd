% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{get_broc_prior}
\alias{get_broc_prior}
\title{Preview the Priors for an SDT Model}
\usage{
get_broc_prior(object, data = NULL, family = NULL, user_priors = NULL, ...)
}
\arguments{
\item{object}{A \code{\link[=broc]{broc()}} model, or a \code{\link[=brf]{brf()}} specification / bare formula.
When a formula or \code{brf}, the model is built internally from \code{data},
\code{family}, and any \code{...}.}

\item{data}{Data frame. Required when \code{object} is a formula or \code{brf}, because
coefficient names and the number of thresholds are read from it. Ignored
when \code{object} is already a model.}

\item{family}{Optional family (e.g. \code{\link[=evsd]{evsd()}}, \code{\link[=uvsd]{uvsd()}}) for the formula/\code{brf}
path; defaults to the family set in the \code{brf}, or \code{\link[=evsd]{evsd()}}. Ignored when
\code{object} is already a model.}

\item{user_priors}{Optional priors from \code{\link[=broc_prior]{broc_prior()}} to override defaults.}

\item{...}{Further arguments forwarded to \code{\link[=broc]{broc()}} on the formula/\code{brf} path
(e.g. \code{encoding_vars}, \code{counts}, \code{cor_threshold}, \code{gap_link}).}
}
\value{
A data frame with columns \code{class}, \code{dpar}, \code{coef}, \code{group}, \code{prior},
and \code{source}, where \code{source} flags whether each row is a package default
(\code{"default"}) or was overridden by \code{user_priors} (\code{"user"}).
}
\description{
Show every prior a model will use as a data frame. Accepts either a built
\code{\link[=broc]{broc()}} model or a \code{\link[=brf]{brf()}} object that specifies a formula, data, and a
family.
}
\examples{
\dontrun{
# Straight from a formula -- no need to build the model first
get_broc_prior(conf | old ~ condition + (1 | subject),
               data = my_data, family = evsd())

# From a brf() specification (family carried on the brf)
spec <- brf(conf | old ~ condition + (1 | subject),
            criterion ~ 1 + (1 | subject), family = uvsd())
get_broc_prior(spec, data = my_data)

# From an already-built model, with custom overrides
model <- broc(spec, data = my_data)
my_priors <- c(
  broc_prior("normal(2, 0.5)", class = "b", dpar = "dprime"),
  broc_prior("normal(0, 1)", class = "sd")
)
get_broc_prior(model, user_priors = my_priors)
}
}
\seealso{
\code{\link[=broc_prior]{broc_prior()}}
}
