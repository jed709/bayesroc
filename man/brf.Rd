% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brf.R
\name{brf}
\alias{brf}
\title{Compose a Multi-Parameter SDT Model Formula}
\usage{
brf(
  formula,
  ...,
  family = NULL,
  encoding_vars = NULL,
  counts = NULL,
  cor_threshold = NULL,
  threads = NULL,
  gap_link = NULL
)
}
\arguments{
\item{formula}{Two-sided formula for the primary parameter (d' / mu),
\code{response | is_old ~ predictors}. \code{is_old} may be coded 0/1 (new/old) or
centered (-0.5/0.5); centered coding is not supported with lure mixtures or
multi-response families. For multi-response families, use \code{\link[=resp]{resp()}} on the
left-hand side.}

\item{...}{Additional one-sided formulas, one per parameter, with the
parameter name on the left. E.g. \code{criterion ~ 1 + (1 | subj)},
\code{sigma ~ cond}, \code{lambda ~ 1}.}

\item{family}{The SDT model family: one of \code{\link[=evsd]{evsd()}}, \code{\link[=uvsd]{uvsd()}}, \code{\link[=dpsd]{dpsd()}},
\code{\link[=mixture]{mixture()}}, \code{\link[=source_mixture]{source_mixture()}}, \code{\link[=bivariate_gaussian]{bivariate_gaussian()}}, \code{\link[=bivariate_dp]{bivariate_dp()}},
\code{\link[=vrdp2d]{vrdp2d()}}, \code{\link[=cdp]{cdp()}}, \code{\link[=cumulative]{cumulative()}}, or \code{\link[=bivariate_cumulative]{bivariate_cumulative()}}. May also be
set in \code{\link[=brf]{brf()}}; a value in broc() takes precedence.}

\item{encoding_vars}{Character vector of column names to treat as
encoding-phase (study) manipulations for which lure items do not have a
meaningful value. Listing a column here auto-wraps it in \code{encoding()}. A
value passed to \code{\link[=broc]{broc()}} takes precedence over one set here.}

\item{counts}{Name of a column giving per-row trial counts, for aggregated
(count) data rather than one row per trial. May also be set in \code{\link[=brf]{brf()}}.}

\item{cor_threshold}{If \code{TRUE} (default), random effects on the K-1 thresholds
are modeled jointly (correlated) via the threshold parameterization; \code{FALSE}
treats them as independent.}

\item{threads}{If \code{TRUE}, enable within-chain \code{reduce_sum} threading for the
Stan backend (set the thread count per chain via \code{fit_broc(threads_per_chain=)}).
Default \code{FALSE}.}

\item{gap_link}{Link for threshold gaps: \code{"log"} (default) or \code{"softplus"}.
\code{NULL} uses the family default.}
}
\value{
A \code{brf} object: the model specification to pass to \code{\link[=broc]{broc()}}.
}
\description{
Formula specification for bayesroc models. The first argument is the formula
for the primary parameter (d' / mu); each additional formula's left-hand side
names the parameter it applies to (e.g. \code{criterion}, \code{sigma}, \code{lambda}).
}
\examples{
# Standard SDT
brf(conf | old ~ cond + (1|subj),
     criterion ~ 1 + (1|subj),
     sigma ~ 1)

# Bivariate with resp()
brf(resp(det_conf, src_conf) | type ~ 1 + (1|subj),
     discrim ~ 1, criterion ~ 1, criterion2 ~ 1, rho ~ 1)

# CDP with resp()
brf(resp(conf, rk_var) | old ~ 1 + (1|subj),
     fam ~ 1, criterion ~ 1, rec_crit ~ 1)

# Cumulative
brf(rating ~ cond, cutpoints ~ 1 + (1|subj))
}
