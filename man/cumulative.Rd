% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\name{cumulative}
\alias{cumulative}
\title{Cumulative Ordinal Regression Model}
\usage{
cumulative(link = link_probit())
}
\arguments{
\item{link}{Link function (default: \code{\link[=link_probit]{link_probit()}}). \code{\link[=link_logit]{link_logit()}} also
supported; GEV link not implemented for this family.}
}
\value{
A \code{broc_family} object.
}
\description{
Ordinal regression with cumulative-link cutpoints and no SDT structure
(Agresti, 2010).
}
\details{
\subsection{Likelihood}{

For an ordinal response Y on a K-point scale with cutpoints
c_1 < ... < c_{K-1}:
\deqn{P(Y \le k \mid x) = F(c_k - \mu(x))}
with \eqn{c_0 = -\infty} and \eqn{c_K = +\infty}, where F is the link CDF
(probit or logit) and \eqn{\mu(x)} is the linear predictor. Cell
probabilities by differencing:
\eqn{P(Y = k \mid x) = F(c_k - \mu(x)) - F(c_{k-1} - \mu(x))}.
}

\subsection{Parameters}{

\describe{
\item{mu}{(required) Linear predictor of covariate effects; the intercept is
absorbed by the cutpoints, so \code{rating ~ 1} has no \code{mu} coefficients.
Identity link.}
\item{cutpoints}{(required) K-1 ordered cutpoints (mid-anchor + K-2 ordered
gaps). Identity link. Set priors via \code{thresh_mid} and \code{log_gaps}.}
}
}

\subsection{Formula syntax}{

\if{html}{\out{<div class="sourceCode">}}\preformatted{broc(brf(rating ~ condition + (condition | subj),
         cutpoints ~ 1 + (1 | subj),
         family = cumulative()),
    data = dat)
}\if{html}{\out{</div>}}
}
}
\references{
Agresti, A. (2010). \emph{Analysis of ordinal categorical data} (2nd ed.). Wiley.
}
\seealso{
\code{\link[=evsd]{evsd()}} for the simplest SDT model;
\code{\link[=bivariate_cumulative]{bivariate_cumulative()}} for the bivariate extension.
}
