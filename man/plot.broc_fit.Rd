% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.broc_fit}
\alias{plot.broc_fit}
\title{Plot Model Parameters}
\usage{
\method{plot}{broc_fit}(
  x,
  class = "b",
  dpar = "dprime",
  group = NULL,
  type = c("interval", "halfeye", "gradient", "dots"),
  prob = 0.95,
  prob_inner = 0.5,
  ...
)
}
\arguments{
\item{x}{A \code{broc_fit} object from \code{\link[=fit_broc]{fit_broc()}}.}

\item{class}{Parameter class to plot: "b" (fixed effects), "sd" (random effect SDs),
or "cor" (correlations)}

\item{dpar}{Character vector of distributional parameters to plot (default
\code{"dprime"}). The valid set depends on the family (see the family
constructor).}

\item{group}{Optional: filter to specific grouping factor(s) for sd/cor plots}

\item{type}{Plot type: "interval" (default, point + interval), "halfeye" (density + interval),
"gradient" (gradient interval), "dots" (quantile dot plot)}

\item{prob}{Probability mass for outer credible interval (default 0.95)}

\item{prob_inner}{Probability mass for inner credible interval (default 0.5)}

\item{...}{Ignored.}
}
\value{
A ggplot object
}
\description{
Create forest plots of parameter estimates with credible intervals from fitted
models, using ggdist.
}
\examples{
\dontrun{
# Plot d' fixed effects with default interval style
plot(fit, class = "b", dpar = "dprime")

# Plot with halfeye (density + interval)
plot(fit, class = "b", dpar = "dprime", type = "halfeye")

# Plot multiple fixed effects, faceted by dpar
plot(fit, class = "b", dpar = c("dprime", "sigma"))

# Plot random effect SDs
plot(fit, class = "sd", dpar = "dprime")

# Plot correlations with gradient intervals
plot(fit, class = "cor", dpar = "dprime", type = "gradient")
}
}
