% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior.R
\name{posterior_linpred.broc_fit}
\alias{posterior_linpred.broc_fit}
\title{Posterior draws of a linear predictor (dpar)}
\usage{
\method{posterior_linpred}{broc_fit}(
  object,
  transform = FALSE,
  newdata = NULL,
  dpar = "dprime",
  re_formula = NULL,
  ndraws = NULL,
  summary = FALSE,
  prob = 0.95,
  allow_new_levels = FALSE,
  seed = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{broc_fit} object from \code{\link[=fit_broc]{fit_broc()}}.}

\item{transform}{Ignored.}

\item{newdata}{Optional data frame. If \code{NULL}, uses the training data.}

\item{dpar}{Distributional parameter to extract (default \code{"dprime"}). Valid
names depend on the family -- the same names used in \code{\link[=brf]{brf()}} and
\code{\link[=broc_prior]{broc_prior()}}.}

\item{re_formula}{\code{NULL} (default) includes random effects; \code{NA} excludes
them (population-level prediction).}

\item{ndraws}{Optional number of posterior draws to subsample. Default
uses all draws.}

\item{summary}{If \code{TRUE}, returns a data frame with \code{mean}, \code{lower},
\code{upper}, \code{trial}. If \code{FALSE} (default), returns the full S x N matrix.}

\item{prob}{CI width when \code{summary = TRUE} (default 0.95).}

\item{allow_new_levels}{If \code{TRUE}, sample random effects from their prior for
grouping levels in \code{newdata} not seen during training. Default \code{FALSE} errors
if \code{newdata} introduces new levels.}

\item{seed}{Optional integer for reproducibility (subsampling and
new-level RE draws). Restores caller's RNG state on exit.}

\item{...}{Ignored.}
}
\value{
If \code{summary = FALSE}, an S x N matrix. If \code{TRUE}, a data frame with
\code{trial}, \code{mean}, \code{lower}, \code{upper}.
}
\description{
Returns posterior draws of a distributional parameter at each row of \code{newdata}
on the response scale.
}
\seealso{
\code{\link[=posterior_epred]{posterior_epred()}} for expected response value;
\code{\link[=predict]{predict()}} for category probabilities.
}
