% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditional_smooths.R
\name{conditional_smooths}
\alias{conditional_smooths}
\title{Conditional Smooth Effects}
\usage{
conditional_smooths(
  fit,
  smooths = NULL,
  dpar = NULL,
  resolution = 100,
  prob = 0.95,
  ndraws = NULL
)
}
\arguments{
\item{fit}{A \code{broc_fit} object from \code{\link[=fit_broc]{fit_broc()}}.}

\item{smooths}{Character vector of smooth terms to evaluate (e.g., "s(age)").
If NULL (default), all smooth terms are evaluated.}

\item{dpar}{Optional distributional parameter to filter to (e.g., \code{"dprime"}).}

\item{resolution}{Number of grid points per covariate (default 100).}

\item{prob}{Credible interval width (default 0.95).}

\item{ndraws}{Number of posterior draws to use (default: all).}
}
\value{
A data frame of class \code{conditional_smooths} with covariate value(s),
parameter, smooth label, estimate, lower, upper.
}
\description{
Evaluate smooth terms at a grid of covariate values using posterior draws.
Returns a data frame with posterior mean and credible intervals for each
smooth, suitable for plotting with ggplot2. Requires a model fit with
\code{s()}/\code{t2()} smooth terms in a parameter formula.
}
