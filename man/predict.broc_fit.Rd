% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict.broc_fit}
\alias{predict.broc_fit}
\title{Posterior Predictions from a Fitted Bayesian ROC Model}
\usage{
\method{predict}{broc_fit}(
  object,
  newdata = NULL,
  type = c("response", "prediction"),
  re_formula = NULL,
  ndraws = NULL,
  summary = TRUE,
  prob = 0.95,
  cores = 1,
  response = 1,
  seed = NULL,
  allow_new_levels = FALSE,
  ...
)
}
\arguments{
\item{object}{A \code{broc_fit} object from \code{\link[=fit_broc]{fit_broc()}}.}

\item{newdata}{Optional data frame for out-of-sample prediction. If \code{NULL}
(default), uses the training data.}

\item{type}{\code{"response"} (default) returns posterior category probabilities
P(Y = k); \code{"prediction"} returns simulated categorical responses.}

\item{re_formula}{Controls random effects inclusion. \code{NULL} (default) includes
all random effects; \code{NA} excludes all (population-level predictions only).}

\item{ndraws}{Number of posterior draws to use. \code{NULL} (default) uses all
available draws.}

\item{summary}{If \code{TRUE} (default), returns posterior mean and credible
intervals. If \code{FALSE}, returns the full S x N x K array of draws.}

\item{prob}{Width of credible intervals when \code{summary = TRUE} (default 0.95).}

\item{cores}{Number of cores for parallel probability computation (default 1).}

\item{response}{Which response dimension. For bivariate families: \code{1} (detection)
or \code{2} (source). For CDP: \code{1} (confidence, default) or \code{2} (R/K/G probabilities).
Ignored for other families.}

\item{seed}{Optional integer for reproducibility. Controls the random
subsampling of \code{ndraws} and (for \code{type = "prediction"}) the categorical
draws used to simulate responses. The previous \code{.Random.seed} is restored
on exit. Default \code{NULL} leaves the RNG untouched.}

\item{allow_new_levels}{If \code{TRUE}, allow \code{newdata} to contain random-effect
levels not seen during fitting; their effects are drawn from the estimated
population distribution. Default \code{FALSE} errors if \code{newdata} introduces new
levels.}

\item{...}{Ignored.}
}
\value{
Depends on \code{type} and \code{summary}:
\describe{
\item{\code{type = "response"}, \code{summary = TRUE}}{A data frame with columns
for each category's posterior mean probability plus lower/upper CI bounds.}
\item{\code{type = "response"}, \code{summary = FALSE}}{An S x N x K array where
S = draws, N = observations, K = response categories.}
\item{\code{type = "prediction"}, \code{summary = TRUE}}{A vector of modal predicted categories.}
\item{\code{type = "prediction"}, \code{summary = FALSE}}{An S x N matrix of
simulated categorical responses.}
}
}
\description{
Generate posterior predictive response probabilities or simulated responses
from a fitted model.
}
\examples{
\dontrun{
fit <- fit_broc(model)
# Category probabilities (summarized)
preds <- predict(fit)
# Full posterior array
preds_full <- predict(fit, summary = FALSE, ndraws = 100)
# Population-level only (no random effects)
preds_pop <- predict(fit, re_formula = NA)
}
}
\seealso{
\code{\link[=pp_check]{pp_check()}} for visual posterior predictive checks.
}
