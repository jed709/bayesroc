% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broc_model.R
\name{get_numpyro_config}
\alias{get_numpyro_config}
\title{Extract the Generated NumPyro (JAX) Config}
\usage{
get_numpyro_config(x, json = FALSE)
}
\arguments{
\item{x}{A \code{broc_model} object created by \code{\link[=broc]{broc()}}.}

\item{json}{If \code{TRUE}, return the pretty-printed JSON string actually written
for the Python backend (matrices expanded to row lists), of class \code{json}.
If \code{FALSE} (default), return the R list.}
}
\value{
The config as a named list, or, when \code{json = TRUE}, a length-one
character vector of class \code{json}.
}
\description{
Returns the config list that \code{\link[=broc]{broc()}} generates for the JAX backend, the
counterpart to \code{\link[=get_stan_code]{get_stan_code()}}/\code{\link[=get_stan_data]{get_stan_data()}}. It describes the model
(family, link, response data, design matrices, priors, threshold and
random-effect structure) and is serialized to JSON for \code{\link[=fit_broc]{fit_broc()}} with
\code{backend = "jax"}.
}
\examples{
\dontrun{
model <- broc(brf(conf | old ~ cond), data = dat, family = evsd())
cfg <- get_numpyro_config(model)
str(cfg)
# Exactly what fit_broc(backend = "jax") sends to Python:
cat(get_numpyro_config(model, json = TRUE))
}
}
\seealso{
\code{\link[=get_stan_code]{get_stan_code()}}, \code{\link[=get_stan_data]{get_stan_data()}}.
}
