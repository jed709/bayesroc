% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broc_model.R
\name{fit_broc}
\alias{fit_broc}
\title{Compile and Fit a Bayesian ROC Model}
\usage{
fit_broc(
  model,
  chains = 4,
  parallel_chains = 4,
  iter_warmup = 2000,
  iter_sampling = 2000,
  thin = 1,
  adapt_delta = 0.8,
  max_treedepth = 10,
  init = 0,
  refresh = 100,
  threads_per_chain = NULL,
  backend = c("stan", "jax"),
  python = NULL,
  progress_bar = TRUE,
  seed = NULL,
  file = NULL,
  file_refit = FALSE,
  save_loglik = TRUE,
  num_warmup = NULL,
  num_samples = NULL,
  num_chains = NULL,
  ...
)
}
\arguments{
\item{model}{A \code{broc_model} object created by \code{\link[=broc]{broc()}}.}

\item{chains}{Number of MCMC chains (default 4).}

\item{parallel_chains}{Number of chains to run in parallel (default 4). For
the JAX backend this is all-or-nothing: chains run in parallel across
devices when \code{parallel_chains >= chains}, and sequentially on one device
when \code{parallel_chains < chains}.}

\item{iter_warmup}{Number of warmup/adaptation iterations per chain (default 2000).}

\item{iter_sampling}{Number of post-warmup sampling iterations per chain (default 2000).}

\item{thin}{Period for saving samples (default 1). \code{thin = k} keeps every
\code{k}-th post-warmup draw.}

\item{adapt_delta}{Target acceptance probability for NUTS (default 0.8).}

\item{max_treedepth}{Maximum tree depth for NUTS (default 10).}

\item{init}{Initial-value strategy. Default \code{0}: all parameters start at 0 on
the unconstrained scale. Other options: \code{"prior"} samples each parameter
from its prior; \code{"random"} uses Stan's U(-2, 2) on the unconstrained scale
(can be unstable for log-linked parameters); a single number \code{x} sets a
U(-x, x) initialization radius; or a function or list of per-chain init lists.}

\item{refresh}{How often the Stan backend prints progress, in iterations
(default 100). Stan only; the JAX progress bar has no refresh-rate control.}

\item{threads_per_chain}{Number of threads per chain for within-chain parallelism
via \code{reduce_sum}. Only used when the model was built with \code{threads = TRUE}.}

\item{backend}{Inference backend: \code{"stan"} (default, via cmdstanr) or \code{"jax"}
(via NumPyro). The JAX backend requires a configured Python environment
(see \code{\link[=install_jax_backend]{install_jax_backend()}}).}

\item{python}{Optional path to the Python executable for the JAX backend.
\code{NULL} (default) uses the managed environment (see \code{\link[=install_jax_backend]{install_jax_backend()}}).}

\item{progress_bar}{If \code{TRUE} (default), show the sampler progress bar.}

\item{seed}{Optional integer seed for reproducible sampling. \code{NULL} (default)
uses a random seed.}

\item{file}{Optional path to save the fitted model to. When set,
the fit is written there with \code{\link[=save_broc_fit]{save_broc_fit()}} once sampling finishes (a
\code{.rds} extension is appended if absent). If the file already exists, the
saved fit is loaded and returned instead of refitting -- unless
\code{file_refit = TRUE}. \code{NULL} (default) does not save.}

\item{file_refit}{If \code{FALSE} (default), an existing \code{file} is loaded and
sampling is skipped; if \code{TRUE}, the model is refit and the file overwritten.
Ignored when \code{file} is \code{NULL}.}

\item{save_loglik}{If \code{TRUE} (default), the per-observation log-likelihood is
kept so the saved fit supports \code{\link[=loo]{loo()}}. If \code{FALSE}, \code{log_lik} is dropped when
the fit is saved.}

\item{num_warmup, num_samples, num_chains}{NumPyro-style aliases for
\code{iter_warmup}, \code{iter_sampling}, and \code{chains}. When supplied, each overrides
its canonical counterpart.}

\item{...}{Additional arguments forwarded to cmdstanr's \verb{$sample()} (Stan
backend only; a warning is issued if any are passed with \code{backend = "jax"}).}
}
\value{
A \code{broc_fit} object. It carries low-level accessors \verb{$draws()},
\verb{$summary()}, \verb{$diagnostic_summary()}, \verb{$loo()}, \verb{$num_chains()},
\verb{$metadata()}, and \verb{$save_object()}, but S3 methods
\link[=summary.broc_fit]{summary()}, \link[=plot.broc_fit]{plot()},
\link[=predict.broc_fit]{predict()}, \link[=pp_check.broc_fit]{pp_check()}, and
\link[=loo.broc_fit]{loo()} are also provided.
}
\description{
Takes a \code{broc_model} object (from \code{\link[=broc]{broc()}}) and runs MCMC sampling via
Stan (cmdstanr) or JAX (NumPyro). Returns a \code{broc_fit} object that wraps
the backend-specific result and provides a uniform interface for downstream
analysis (\code{summary(fit)}, \code{predict(fit)}, \code{pp_check(fit)}, etc.).
}
\examples{
\dontrun{
model <- broc(brf(conf | old ~ cond + (1|subj),
                  sigma ~ 1), data = dat, family = uvsd())
fit <- fit_broc(model)
fit <- fit_broc(model, backend = "jax", chains = 2)

# Save on first run; reload instead of refitting on later runs.
fit <- fit_broc(model, file = "my_fit")          # writes my_fit.rds
fit <- fit_broc(model, file = "my_fit")          # loads my_fit.rds
fit <- fit_broc(model, file = "my_fit", file_refit = TRUE)      # refits
}
}
\seealso{
\code{\link[=broc]{broc()}} for model specification, \code{summary(fit)} for results.
}
