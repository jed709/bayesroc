% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/broc_fit.R
\name{save_broc_fit}
\alias{save_broc_fit}
\title{Save a fitted broc_fit to disk}
\usage{
save_broc_fit(
  fit,
  file,
  compress = FALSE,
  verbose = TRUE,
  include_loglik = NULL
)
}
\arguments{
\item{fit}{A \code{broc_fit} object from \code{\link[=fit_broc]{fit_broc()}}.}

\item{file}{Path to write to (use \code{.rds} extension by convention).}

\item{compress}{Compression for \code{saveRDS}. Default \code{FALSE} (fast; posterior
draws barely compress). Set \code{"gzip"}/\code{"xz"} for a smaller, slower file.}

\item{verbose}{If \code{TRUE} (default), print a brief progress message.}

\item{include_loglik}{Whether to write \code{log_lik} (needed for \code{\link[=loo]{loo()}}). \code{NULL}
(default) follows the fit's \code{fit_broc(save_loglik = )} setting; \code{TRUE}/\code{FALSE}
overrides it for this save.}
}
\value{
Invisibly returns \code{file}.
}
\description{
Writes a portable .rds containing the draws, the broc_model, and the metadata
needed to reconstruct the fit. Pass \code{file} to \code{\link[=fit_broc]{fit_broc()}} to save
automatically, or call this on an existing fit. Always save with this (or
\verb{file =} in \code{\link[=fit_broc]{fit_broc()}}), not \code{saveRDS()}, which can lose Stan draws.
}
\examples{
\dontrun{
fit <- fit_broc(model)
save_broc_fit(fit, "my_fit.rds")
fit2 <- load_broc_fit("my_fit.rds")
summary(fit2)

# Smaller file (slower) when disk space matters:
save_broc_fit(fit, "tmp.rds", compress = "gzip")
}
}
\seealso{
\code{\link[=load_broc_fit]{load_broc_fit()}}.
}
